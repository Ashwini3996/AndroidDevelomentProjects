<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="test.voicerecognitiontest">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />


    <application
        android:allowBackup="true"
        android:icon="@drawable/email_splash"
        android:label="@string/app_name"
        android:roundIcon="@drawable/email_splash"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"/>

        <activity android:name=".GmailAcc"
            android:label="Gmail API Android Quickstart" >
        </activity>

        <activity android:name=".ComposeEmail"/>

        <activity android:name=".ActivityTextToSpeech"/>

        <activity android:name=".EmailDisplayActivity"/>
    </application>

</manifest>